'use client';
import React from 'react'

const AddToCart = () => {
  return (
    <div>
        <button onClick={() => console.log('Click!')}>Click Me</button>
    </div>
  )
}

export default AddToCart